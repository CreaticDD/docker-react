language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t donutcoffee/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true donutcoffee/docker-react npm run test -- --coverage